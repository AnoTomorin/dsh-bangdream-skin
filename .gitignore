# Stray profile manifest file (not part of the plugin package)
@{name=dsh-profile-web*

# Archives / build artifacts
*.zip
*.tgz
*.tar.gz

# Node / dependencies
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
